/** Shown in a separate info popover on the edit-employee form (keeps the main description short). */
export const EDIT_EMPLOYEE_EMAIL_CHANGE_DETAILS =
  "Update profile and email. Changing email clears verification until they confirm the new address, ends their sessions, and sends a new verification message. Google sign-in only works if their Google account uses the same email."
